@extends('layouts.admin')

@section('title')
    Pending Resources - Admin Review
@endsection

@section('content')
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <h4>Pending Resources for Review</h4>
                        <p class="text-muted">Documents submitted by users awaiting admin approval</p>
                    </div>
                    <div class="card-body">
                        @if($pendingResources->count() > 0)
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>Title</th>
                                            <th>Author</th>
                                            <th>Type</th>
                                            <th>Field</th>
                                            <th>Submitted</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach($pendingResources as $resource)
                                            <tr>
                                                <td>
                                                    <strong>{{ $resource->title }}</strong>
                                                    @if($resource->sub_fields)
                                                        <br><small class="text-muted">Fields: {{ $resource->sub_fields }}</small>
                                                    @endif
                                                </td>
                                                <td>
                                                    @if($resource->user)
                                                        {{ $resource->user->first_name }} {{ $resource->user->last_name }}
                                                        <br><small class="text-muted">{{ $resource->user->email }}</small>
                                                    @else
                                                        <span class="text-muted">Unknown User</span>
                                                    @endif
                                                </td>
                                                <td>{{ ucfirst($resource->type ?? 'N/A') }}</td>
                                                <td>{{ ucfirst($resource->field ?? 'N/A') }}</td>
                                                <td>
                                                    @if($resource->submitted_at)
                                                        {{ \Carbon\Carbon::parse($resource->submitted_at)->format('M d, Y H:i') }}
                                                    @else
                                                        N/A
                                                    @endif
                                                </td>
                                                <td>
                                                    <a href="{{ route('admin.resources.show', $resource->id) }}" class="btn btn-sm btn-primary">
                                                        <i class="fas fa-eye"></i> Review
                                                    </a>
                                                </td>
                                            </tr>
                                        @endforeach
                                    </tbody>
                                </table>
                            </div>
                        @else
                            <div class="text-center py-5">
                                <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                                <h5>No Pending Resources</h5>
                                <p class="text-muted">All submitted documents have been reviewed.</p>
                            </div>
                        @endif
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection

@section('css')
    <style>
        .table th {
            background-color: #f8f9fa;
            border-bottom: 2px solid #dee2e6;
        }
        .btn-sm {
            padding: 0.25rem 0.5rem;
            font-size: 0.875rem;
        }
    </style>
@endsection
